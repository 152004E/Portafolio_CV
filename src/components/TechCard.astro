---
interface Props {
  name: string;
  level: string;
  icon: string;
  description: string;
}

const { name, level, icon, description } = Astro.props;

function getLevelColor(level: string) {
  if (level === "Avanzado") return "bg-emerald-500/15 text-emerald-400 border-emerald-500/25";
  if (level === "Intermedio") return "bg-blue-500/15 text-blue-400 border-blue-500/25";
  return "bg-yellow-500/15 text-yellow-400 border-yellow-500/25";
}
---

<div class="group relative overflow-hidden rounded-2xl border border-white/10 bg-white/[0.02] backdrop-blur-md p-4 transition-all duration-300 hover:-translate-y-1 hover:border-blue-500/40 hover:shadow-[0_0_25px_rgba(59,130,246,0.1)]">
  <div class="absolute -top-20 -right-20 w-40 h-40 bg-blue-500/5 rounded-full blur-3xl group-hover:bg-blue-500/10 transition-all duration-500"></div>
  <div class="relative z-10 flex items-start gap-3">
    <div class="flex h-9 w-9 shrink-0 items-center justify-center rounded-lg bg-blue-500/10 text-blue-400 text-base group-hover:bg-blue-500/20 group-hover:text-blue-300 transition-all duration-300">
      <i class={icon}></i>
    </div>
    <div class="flex-1 min-w-0">
      <div class="flex items-center justify-between gap-2">
        <h4 class="text-white font-semibold text-sm">{name}</h4>
        <span class={`shrink-0 px-2 py-0.5 rounded-full text-[10px] font-bold uppercase tracking-wider border ${getLevelColor(level)}`}>
          {level}
        </span>
      </div>
      <p class="text-gray-500 text-xs mt-1 leading-relaxed line-clamp-2">
        {description}
      </p>
    </div>
  </div>
</div>
